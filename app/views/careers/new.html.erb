<div class="container" style=" margin-top: 100px;" >
  <div class="text-center">
    <% if current_user.careers.present? %>
      <h3>経歴作成ページ</h3>
    <% else %>
      <h3>続いて、あなたの直近の経歴を登録してください。</h3>
    <% end %>
  </div>

    <%= render 'form', career: @career %>

  <div class="text-center">
    <%= link_to 'Back', user_path(current_user), class: "text-dark btn btn-outline-secondary" %>
  </div>
</div>
